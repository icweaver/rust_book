fn main() {
    let num = 1;
    let val = 5;

    let x = if num > val {
        1
    } else if num < val {
        0.0
    } else {
        -1
    };

    //dbg!(x);

    // let x = if num > 5 {10} else {"low"};

    // println!("{x}");
}
